这次调通的过程本质上是从“全局收紧”改成“绿色定向补偿”。

**1. 这几轮我都改了什么**
按阶段回顾（都在 main.py）：

1. 初始稳噪阶段  
- 收紧了 HSV 阈值（红绿黄）main.py 到 main.py  
- 加了形态学闭运算 main.py 到 main.py  
- 提高了 Hough 与像素密度判定阈值 main.py 到 main.py，main.py，main.py，main.py  
- 结果：噪声下降，但出现漏检。

2. 回调可检出阶段  
- 放宽绿色相关阈值（HSV、Hough、h/s）main.py，main.py，main.py  
- 结果：绿色回来了，但噪声和多检上升。

3. 收敛阶段（平衡噪声与漏检）  
- 绿色范围小幅收紧，形态学核从 7x7 回到 5x5 main.py，main.py  
- 绿色 Hough 去重能力增强（minDist、param2）main.py  
- 绿色 h/s 稍提高 main.py  
- 结果：噪声与多检下降，但“正对绿灯发黄”仍漏。

4. 定向补偿阶段（最终关键）  
- 新增绿色过曝补充掩码：低饱和+高亮区域 main.py 到 main.py  
- 与主绿色掩码做并集 main.py 到 main.py  
- 结果：解决“刚进镜头能识别、对焦后发黄识别不到”的核心问题。

**2. 最有效的一步是哪一步**
最有效的是第 4 阶段：  
- 新增 lower_green_wash / upper_green_wash，并与 maskg 做 bitwise_or。  
对应位置是 main.py 到 main.py 和 main.py 到 main.py。

原因：  
- 你的真实问题不是“绿色不存在”，而是“绿色在对焦后过曝，S 下降、V 升高，视觉偏白黄”。  
- 以前只有常规绿色区间，过曝绿灯掉出了主掩码；补充区间把这类像素捞回来了。

**3. 未来可调参数都在哪，以及怎么调**
下面是实用调参手册（按优先级）：

1. 绿色主 HSV 区间（常规绿灯）  
位置：main.py 到 main.py  
- 想减少绿噪声：提高 S 下界（第二个值）或收窄 H 上下界（第一个值）  
- 想减少绿漏检：降低 S/V 下界（第二、第三个值）

2. 绿色过曝补充区间（发白发黄绿灯）  
位置：main.py 到 main.py  
- 对焦后识别不到：降低 S 下界、提高 V 下界  
- 背景误检偏多：提高 V 下界，或把 H 范围收窄（比如把下界往 35-40 提）

3. 形态学核大小  
位置：main.py  
- 噪声碎点多：核变大（5x5 -> 7x7）  
- 连成大块导致误检：核变小（7x7 -> 5x5 或 3x3）

4. 绿色 Hough 圆检测参数  
位置：main.py 到 main.py  
- param2：越大越严格，噪声少但更易漏检  
- minDist：越大重复框越少，但近距离双灯可能少检  
- minRadius/maxRadius：按画面里灯的实际像素尺寸限制，可明显减误检

5. 绿色最终密度判定阈值 h/s  
位置：main.py  
- 提高：过滤噪声和假阳性  
- 降低：提高检出率（尤其远距、小灯、过曝灯）

6. 画面搜索区域限制  
位置：main.py 和 main.py、main.py、main.py  
- bound 越小：只看更靠上的区域，噪声少但可能漏低位灯  
- bound 越大：能检更多位置，但背景误检会上来

**4. 一套通用调参顺序（建议固定流程）**
1. 先调绿色主区间与过曝补充区间（先颜色再几何）  
2. 再调绿色 Hough 的 param2 与 minDist  
3. 最后只微调绿色 h/s 阈值（每次改 2 到 5）  
4. 核大小放在最后动，避免它掩盖前面参数效果
